let x = 1.60; // fica difícil de enteder o q essa variável representa.

// Então devemos escolher nomes que dê sentido as coisas.

let alturaDoDouglas = 1.60;
console.log(alturaDoDouglas);

let altura = 7;
let largura = 10;
let area =  altura * largura;

// Letras maipusculas e minúsculas fazem diferença.
let preco = 10;
let Preco = 20;

console.log(preco);
console.log(Preco);

let idadeUsuario = 20;
let nomeCompleto = 'Douglas Miranda';