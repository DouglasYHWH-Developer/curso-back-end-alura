//  8. Modularização com export/import
//  Crie uma função simples chamada somar(a, b) e exporte-a como módulo 
// (modo CommonJS ou ES Modules, dependendo do ambiente).n 

export function soma(a, b){
    return a + b
}